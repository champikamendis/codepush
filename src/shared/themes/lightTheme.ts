import {AppTheme} from '../constants';
import lightColors from './typography/colors/lightColors';
import lightSizes from './typography/sizes/lightSizes';

const lightTheme: AppTheme = {
  dark: false,
  colors: lightColors,
  sizes: lightSizes,
};

export default lightTheme;
